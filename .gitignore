*.bak
*.tmp
.DS_Store
Thumbs.db

# Secrets — never commit API keys
.env
.env.*
**/.env
**/.env.*
decompiled/

# internal Nexus release tooling — local only, not for the public dist repo
tools/nexus-publish/

# Local Claude Code config/agents — off git
.claude/

# claude-mem data leaked into cwd via a Windows-style path (C:\Users\...) — never track
C:\\Users\\Jooshua\\.claude-mem/
