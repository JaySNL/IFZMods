# Release queue — pending Nexus uploads

Single source of truth for what is **built and staged but not yet pushed to Nexus**.
The updater reads THIS file instead of scanning every mod folder.

Canonical per-mod metadata — version, `fileNotes`, `dllPath`, `nexusModId`, `requires`,
description — lives in `mods.json`, keyed by the same `key`. This file only tracks **what
changed this cycle and where each mod is in the pipeline**; pull the rest from `mods.json`.

**Status flow:** `BUILT` (DLL staged in `plugins/`) → `VERIFIED` (checked in-game) →
`PUSHED` (live on Nexus). Only push a mod once it is `VERIFIED`.

**To push one entry — use the single orchestrator (do NOT run the steps piecemeal):**
1. `node publish-all.mjs --send <Key>` — runs the whole pipeline in order, stopping on failure:
   upload DLL (`fileNotes`) → **Requirements tab** (`requires` → IFZModAPI links) → page version + description.
   Preview first with no `--send`. The Requirements step is the one that gets dropped when you hand-run
   `nexus-upload` + `push-details-api` separately — that's how RaiderEscalation 1.2.6 first shipped without
   its API dependency. Run the orchestrator so it can't be skipped.
2. Move the entry to **Pushed** below and bump its status.

Headless by default (no Chrome). `--no-details` skips the page-details step; `--browser-details` uses the
legacy Playwright path. Auth: `NEXUS_API_KEY` + `NEXUS_COOKIE`/`NEXUS_UA` in `.env.local`.

---

## Pending

- **Unlocked Buildings → 1.3.3** (mod 38) — **PUSHED 2026-07-14** (`publish-all --send`: upload ✓, requirements [204], changelog [200], details [200]). **Small buildings can now be split.** The Split button stayed greyed-out on small buildings (Surface < 200) even with `AllowSmallSurface` on because `SplitConfig.IsTooSmallForSplit` rejected them before the split. Fix: a Postfix now yields that gate to the toggle, so small buildings split.

- **Surrounded → 0.4.6** (mod 55, file 7622861) — PUSHED 2026-07-06 (`publish-all --send`: upload ✓, requirements API ≥1.7.0 [204], changelog [200], details [200]). HiveMind verified in-game. (master 7bf8844 + 160c1e5). **HiveMind: any attack near a cluster wakes it to swarm the attacker** (was out-of-range-shells only). `ShelledReaction`'s idle-gate moved off seed detection (kept on cluster membership) → a group hit by *bullets* (which makes it acquire the shooter) now also seeds; new `HivemindMinCluster` (default 3) gates the surge to real clusters; the hive's target = the seed's acquired target (the shooter) via `SetTarget(temporary:false)`, else nearest structure/HQ for shells. Reliable pursuit doubles as a kiting tool. Config: `ShellDamageThreshold`→`AttackDamageThreshold` (rename orphans the old key). No Harmony patch. Requires IFZ Mod API (unchanged). **Do NOT push until the 6-step in-game check passes** (swarm-attacker / straggler-safe / shell-fallback / min-cluster knob / stale-target / no zero-rva). Spec+plan: `docs/superpowers/{specs,plans}/2026-07-05-surrounded-hivemind*`. Before push: bump `mods.json` 55 → 0.4.6 + fileNotes.




- **RaiderEscalation → 1.3.1-beta** (mod 57) — **PUSHED 2026-07-06** (`publish-all --send`, upload ✓ file 7622597, requirements [204] API ≥1.6.2, changelog [200], details [200]) as a **beta, still awaiting in-game verify** (user directed the beta push before verifying). **Wipe detection now instant; RaidForce scales with fight one-sidedness.** The raid force never climbed because wipe detection counted per-raider death callbacks, which missed some kills (explosions, friendly fire) — so real camp wipes fell through to the timeout and were misread as false "linger" events. Fix: wipe detection now polls live raider headcount, instantly recognizing zero-raider state. RaidForce scaling on wipe reworked: a fast/bloodless clear gives a full escalation step, while a slow/costly fight barely moves it, interpolated via new `KillPain` config (default 0.5, weight per player squad death against escalating). Deprecates `FastWipeSeconds` (old timeout fallback now dead, detection is instant). Net effect: vehicles and larger raids now actually escalate through normal play. Requires IFZ Mod API (unchanged). Next: confirm in-game that a fresh wipe shows `wipe=True margin → RaidForce` climbing.

- **Unlocked Buildings → 1.3.2** (mod 38) -- PUSHED to Nexus (file .7z v1.3.2, changelog [200], details [200], tags=2; jumped live 1.3.0 → 1.3.2, the never-pushed 1.3.1 skipped). **Gathering Place capacity fix.** The forum building's "Places for citizens" capacity was hard-capped at ~2000, ignoring the per-building volume scaling and UncapBuildingCapacity multipliers applied to all other stats. Root cause: `LawsConfig.CalculateForumCapacity` evaluates an AnimationCurve that saturates at ~2000 and bypasses the per-building clamp/uncap logic. Fix: new Harmony postfix on `CalculateForumCapacity` multiplies the curve result by `VolumeMultiplier` whenever `UncapBuildingCapacity` is enabled (so 2000 → ~6000 at default 3x multiplier; raising `VolumeMultiplier` scales it further). Opt-in, bundled with existing BuildingCapacity uncap, sole patcher (no collisions).

---

## Pushed (recent — prune after a few cycles)

- **IFZ Mod Panels → 0.1.3** (mod 77) — **PUSHED 2026-07-26** (`publish-all --send`: upload ✓ file 316 (.7z), requirements [204] API ≥1.9.2, changelog [200], details [200]); superseded 0.1.2 (313) archived. Dev master `a5e22e82`. Pushed before the in-game verify, on the user's call. **Universal gameplay gate: no panel is ever on screen outside live gameplay.** The canvas hangs off the `DontDestroyOnLoad` host, so it OUTLIVED the Game scene — panels stayed up over the main menu — and it was built on `sceneLoaded("Game")`, which fires while the loading screen still covers the scene and the session bindings are half-installed. `PanelsRuntime` now polls a readiness test 5x/sec (`GameReady()`: active scene is `Game`, `!SaveHandler.IsLoadingSave`, `!SaveHandler.LoadingData` — cleared in `MarkLoadingSaveAsFinished`, and only ever set for a save-load so a new game is unaffected — `Cache.Container != null`, and `Cache.TryResolve<TimeController>() != null`, the same "scene fully resolved" proof IFZModAPI uses) and toggles the CANVAS, not each window's `Visible`, so persisted open/closed state survives the menu round-trip. While not ready, toggle keys and consumer `Every()` callbacks are skipped too — a hotkey can't open a panel over the menu. Fixes it for every consumer at once (QoL's housing panel had its own force-hide workaround for this). No public surface change → consumers need no rebuild. Followed by `push-requirements.mjs --send --rebump ElderPop` → [204] (Panels ≥0.1.3). **Not verified in-game.**

- **IFZ Quality of Life → 1.7.1** (mod 33) — **PUSHED 2026-07-26** (`publish-all --send`: upload ✓ file 317 (.7z), requirements [204] API ≥1.9.2 + Panels ≥0.1.3, changelog [200], details [200]); superseded 1.7.0 (267) archived. Dev master `a5e22e82`. **Master toggle now gates the whole mod, live.** `General/Enabled` was honoured by every feature's hot path except `BuildingIconToggle`, whose `Update()` polled the hotkey unconditionally and whose two `StructureIcon` postfixes enforced `BuildingIconsHidden` regardless — so with QoL "off" the `I` key still worked and icons left hidden stayed hidden with no live feature to restore them. Two other things outlived a flag check: the housing panel stayed registered (its 2s low-beds warning kept firing and could still open on a housing click) and nothing reacted to the toggle mid-session. Fixes: master check in `BuildingIconToggle.Update` + both postfixes + `HousingPanel.Show`/`EvaluateWarning`; new `HousingPanel.Unregister()`; and `Enabled.SettingChanged` now registers/unregisters the panel and re-runs `StructureIconHook.RefreshAll()` so flipping the toggle takes effect without a restart. **Not verified in-game.**

- **IFZ Mod API → 1.9.2** (mod 42, file 315) — **PUSHED 2026-07-26** (`publish-all --send`: upload ✓ (.7z), requirements N/A [standalone], changelog [200], details [200]); superseded 1.9.1 (294) and the stale 1.7.0 (289) archived, so 1.9.2 is the only MAIN. Followed by `push-requirements.mjs --send --rebump ElderPop ExtendedHealth` → `[204]` each (API ≥1.9.2; ElderPop's Panels ≥0.1.2 anchor preserved). **One-line fix in `SicknessCoordinator.OnsetReadiness`:** the onset latch is persisted through `SaveStore`, which keys on the map COORDINATES alone, so a colony started on an already-played tile restored the previous colony's latch. That latch is in the new colony's future → `since = runDay - _onsetDay` goes negative → `since < OnsetGraceDays` returns `0f` → readiness pinned at 0 and EVERY registered source (ElderPop frailty, ExtendedHealth) silently suppressed until the new colony's RunDay passed the old onset day + grace. Guard: `if (_onsetDay > runDay) _onsetDay = -1;` (drop + re-latch). Additive, zero public-surface change — consumers need no rebuild, though a full `./build.sh` ran anyway per the MVID rule. Found by the audit below. **Not verified in-game.**

  Audit of the other `SaveStore` consumers (the coords-key collision is generic, not an ElderPop bug): **`SplitFloors`** (proto, `build-exclude`) keyed its records on world POSITION — identical between two colonies on one tile, so it would re-spawn a foreign colony's split floors onto untouched buildings; now stamps `#day=<RunDay>` and discards a later-stamped blob. **`ArmyExtended`** (proto, not in mods.json) resumed a foreign `MissionState`; new primitive `SavedDay` field + same discard rule. Both fixed in dev only — neither ships today, so no release.

- **Elder Population (ElderPop) → 0.1.2** (mod 81, file 7715772) — **PUSHED 2026-07-26** (`publish-all --send`: upload ✓ (packed .7z), requirements [204] API ≥1.9.1 + Panels ≥0.1.2, changelog [200], details [200]); superseded 0.1.1-hotfix (file 253) archived. Dev master `d975a6fd` (+ the SeedMinAge default change). Pushed on the user's call **before** the in-game verify — the two-new-games-on-one-tile repro is still unconfirmed live. Two player-reported fixes (leftkeh) + one default change:
  1. **New colonists sometimes seeded old.** `IFZModAPI.SaveStore` keys its sidecar on the map **coordinates alone**, and the game's `Character.Id` comes from `IdProvider` (`"{prefix}{_lastID}"`, counter restarts at the same value every new colony) — so a second colony on the **same map tile** loaded the previous colony's anchor blob and matched its ages onto the new colonists id-for-id (day-1 elders, plus a pre-retired citizen: the "Retired: 1" on day 1 in the report). `NewGameSeedMaxAge` therefore only held on the first colony ever played on a tile. Fix: blob format v3 adds a `day=<runDay>` stamp; a blob stamped at a **later** day than the day we load at is foreign and dropped. Undateable v2 blobs on a new game are pruned instead: anchors with `birthDay < -(NewGameSeedMaxAge * DaysPerYear)` (seeded above the young band, elapsed-day independent so a raised `NewGameMaxDay` can't re-seed legitimately-aged citizens) and retirees whose anchor can't reach `RetireYears - RetireYearsVariance`.
  2. **Panel hotkey was undiscoverable and fixed.** New `Panel/ToggleKey` rebindable `KeyboardShortcut` (default **F11**) in F1; the panel title now reads `ElderPop (F11)` and the startup log names the key. Rebinding is live (re-`Panels.Register`, no restart).
  3. **`SeedMinAge` default 18 → 12** (youngest seeded adult + child-band ceiling; matches `CharactersConfig.adultAge`). Existing config files keep their value — Reset in F1 to adopt it.
  **Same latent sidecar collision affects the other 3 SaveStore consumers** (SplitFloors, ArmyExtended, `IFZModAPI.SicknessCoordinator`) — audited per-mod rather than changing the API key (an API-level key change would orphan every existing sidecar); see the audit note below this entry.

- **IFZ Mod Panels → 0.1.2** (mod 77, file 7715691) — **PUSHED 2026-07-26** (`publish-all --send`: upload ✓ (packed .7z), requirements [204] API ≥1.9.1, changelog [200], details [200]). **Minimizing a panel left it permanently empty.** Player repro (leftkeh) on ElderPop's panel; defect is in the shared library, so every mod panel was affected. `ModWindow.ApplyCollapsed` overwrote the stored open height with `HeaderHeight`, so un-collapsing computed `Max(HeaderHeight + RowHeight, folded)` = a one-row window; `ResizeGrip` then wrote `sizeDelta` directly with no collapsed check, so dragging it grew the frame while `_bodyViewport` stayed inactive → full-size window, header + grip, no rows/scrollbar. `CaptureState` persisted that as `{h: full, collapsed: true}`, and since `PanelContext.Window()` applies stored state BEFORE the consumer's builder adds rows, every Game-scene load (new game without exiting, or restart) rebuilt the panel with its rows parented under a deactivated viewport. Fix: `_expandedHeight` memo (set on Build, on resize, and on collapse; restored on expand), `ResizeGrip.Grabbed` (new `IBeginDragHandler`) un-collapses before resizing, `CaptureState` persists the open height. Legacy `windows.json` with `h == HeaderHeight` falls back to the built height → affected installs self-heal; 0.1.1 workaround was one click on `–`. Public surface additive (one field on `ResizeGrip`) — consumers need no rebuild/republish. **NOT verified in-game** (built green, deployed; user-side check pending).

- **IFZ Mod API -> 1.9.1** (mod 42, file 294) -- PUSHED 2026-07-09 (`publish-all --send`: upload [file 294 created], requirements N/A [standalone, own file], changelog [200], details [200]). Followed by `push-requirements.mjs --send --rebump GreenhouseGrow` -> `[204]` re-anchored GreenhouseGrow (72) file 7636670 to API >=1.9.1. Live Nexus API had been stuck at 1.7.0 while `IFZModAPI.ProductionUI` (shared production-row `RefreshBar` hook) only ever existed in dev 1.8.x/1.9.x and was never pushed -- players on the old API hit `TypeLoadException: Could not resolve type ... 'IFZModAPI.ProductionUI'` at plugin Awake whenever Expanded Farming (GreenhouseGrow) 0.1.6+ loaded (confirmed from a player's Player.log). Adds `Production.Register` custom-recipe API (sole patcher of `ProductionWork.HasResourcesToProduce` + `ProductionBar.RefreshBar`) + `ProductionUI.RowRefreshed` event + `Cache.WeaponFired` event (Gunfire Lights migrated off its own patch onto this). 1.8.x was never shipped either; 1.9.1 supersedes it and bundles everything since 1.7.0. Additive/backward-compatible: full public-surface diff (ilspycmd decompile of the old vs new DLL, every shared file) showed zero removed or changed members, only additions. Re-staged the real 1.9.1 dll into dist `plugins/000_IFZModAPI.dll` (dist copy was stale 1.7.0, verified zero "ProductionUI" strings before the swap). Also fixed `mods.json`: GreenhouseGrow's `requires` was `null` (left over from its 0.1.2 "standalone" days) despite 0.1.6+ hard-needing the API -- now `["IFZModAPI"]`. Followed by re-anchoring **GreenhouseGrow (72) Requirements tab to API >=1.9.1** via `push-requirements.mjs --send --rebump` so future installers are told to get the right API floor.

- **Expanded Farming (GreenhouseGrow) -> 0.1.7** (mod 72, file 287) -- **PUSHED 2026-07-09** (`publish-all --send`: upload done file 287, requirements N/A [standalone], changelog [200], details [200] after one retry past a transient Cloudflare 403). Jumped live 0.1.5 -> 0.1.7 (0.1.6 was built in dev but never pushed to dist/Nexus). **0.1.7 fixes farm worker count breaking after loading a save**: on reload the game rebuilds a farm's production work with the vanilla worker cap before the mod's driver re-applies the scaled footprint, so saved workers beyond vanilla were dropped -- worst in manual work mode (auto mode masked it via continuous rebalancing). The driver now reasserts the scaled cap and refills workers once per farm after each load; verified in-game (x3 farm cap/workers 2->18, x2 farm cap 2->8 workers 2->6, correctly pool-limited). **0.1.6** (bundled, never separately pushed): farm workers now respect the raised max-staff cap on scaled-up farms. Standalone (no `requires` -> requirements step N/A). Dev repo source for 0.1.7 was uncommitted at push time (Plugin.cs/FarmSizeDriver.cs/csproj modified on dev master, not yet committed there).

- **NoPath - YesPath (DemolishAnywhere) -> 0.3.1** (mod 68, file 7635050) -- **PUSHED 2026-07-09** (`publish-all --send`: upload done, requirements [204] API >=1.7.0, changelog [200], details [200]). First Harmony patch in this mod: sole postfix on `Building.HasMarkerWithBlockedDeconstruction()`. Fixes abandoned buildings with a stuck event lock (e.g. bandit-ransom, blockBuildingDeconstruct=true) that had their demolish button hidden and action blocked (native `NewStructurePanelInfo.IsDeconstructAllowed` + `HandleInfoPanelAction`). Mode-aware: in SANDBOX (`GameCustomize.StoryEvents == false`) the orphaned event-lock is overruled so the building demolishes; in STORY mode (`StoryEvents == true`) the native lock is preserved, live event content untouched. Gated by the existing master `enabled` toggle; fail-safe preserves the vanilla block on error. Verified in-game by the user (both modes). Requires IFZ Mod API (unchanged).

- **Smart Worker Redistribution (SmartWorkerRedist) → 1.2.1** (mod 37, file 7634570) -- **PUSHED 2026-07-09** (`publish-all --send`: upload ✓, requirements [204] API >=1.7.0, changelog [200], details [200]). Removed base-area loose-pile sweep (role now covered by Squad Auto Behavior base-scavenge feature); core redistribution and cancelled-build-refund → debris haul unchanged. Requires IFZ Mod API (unchanged).

- **Squad Auto Behavior (SquadAutoBehavior) → 1.2.0** (mod 39, file 7634573) -- **PUSHED 2026-07-09** (`publish-all --send`: upload ✓, requirements [204] API >=1.7.0, changelog [200], details [200]). Base-scavenge (opt-in via `[SCAV]` tag): idle daytime squads walk to loose resource piles within Radius of HQ, collect them, return to unload; combat-hold/heal-return/ammo-return take priority. Wounded-garrison relocation: wounded squads in non-hospital buildings walk to nearest hospital. New `Scavenge` config (Enabled/NameTag/Radius/DaylightOnly). Requires IFZ Mod API (unchanged).

- **Night Lights (WindowGlow) → 1.1.0** (mod 66, file 283) -- **PUSHED 2026-07-08** (`publish-all --send WindowGlow`: upload ✓, requirements N/A [standalone], changelog [200], details [200]). Nightly window reshuffle: each in-game night, a different set of windows lights up, seeded by in-game day number via new shader uniform `_LitSeed` (applied only mid-day so swap never visible mid-night). Deterministic and reload-stable; lit count unchanged (only which windows change). New config `[Lit] VaryNightly` (default true); set false to restore fixed-every-night. No new Harmony patches, no IFZModAPI change. Standalone (no `requires`).

- **GreenhouseGrow → 0.1.5** (mod 72, file 282) -- **PUSHED 2026-07-08** (`publish-all.mjs --send GreenhouseGrow`: upload ✓, requirements N/A [standalone], changelog [200], details [200]). Playtester-reported bugfixes: farms no longer default to max size (PlaceBump tier resets to x1 on each new placement); greenhouse workers no longer spawn outside the building (work-position picker's double-application of localScale via TransformPoint cancelled for scaled greenhouses only, open fields and collision validator untouched). Standalone (no `requires` -> requirements step N/A).

- **No Map Limit - Buildings → 1.0.0** (mod 85, NEW page — DRAFT, user publishes) — built, deployed and uploaded to Nexus (DLL + page details) 2026-07-08, standalone (no requirements step). Dev repo commit master 8bdff18. **Removes/scales the map building count limit** to allow large real-world areas. IFZ's map selection screen rejects tiles with too many OpenStreetMap buildings (hardware-dependent cap: 20k–50k by benchmark). Mod scales limit by configurable `Multiplier` (default x10, yielding 200k–500k) via sole Harmony patcher on `MapTiler.TileValidation.Requirement.BuildingsCount.Validate`. Config: `[General] Enabled` + `Multiplier` (1-1000). Verified: 60k-building area now playable. Standalone (no `requires`). Still a DRAFT page — user publishes + sets main image/banner.

- **GreenhouseGrow → 0.1.4** (mod 72, file 276) -- **PUSHED 2026-07-07** (`publish-all --send`: upload ✓, requirements N/A [standalone], changelog [200], details [200]). Dev master 41f5549. Display-only fix: the production panel now shows the scaled per-cycle output for enlarged farms (a 4x greenhouse displays ~16x the recipe amount) instead of the flat base recipe number, so the size scaling a farm already gets (since 0.1.2) is visible in the UI. Standalone (no `requires` -> requirements step N/A).

- **IFZQualityOfLife → 1.7.0** (mod 33, file 7627325) — **PUSHED 2026-07-07** (`publish-all --send`: upload ✓, requirements [204] API ≥1.7.0 + Panels ≥0.1.1, changelog [200], details [200]). Dev master. New `AreaWorkMarker/Mode` (NightFade default / Off / Vanilla): the vanilla full-bright blue "queued for area work" fill — trees/debris/streetlights/wrecks queued for cutting AND metal/resource nodes queued for scavenging — beacons at night. NightFade fades it to 0 at night, full by day (keeps daytime cue); Off = never; Vanilla = unchanged. Two `_AreaWorkFill` paths: `GatherableObject.SetFillVisualization(float)` Prefix scales the arg; `Resource.SetFillVisualization(bool)` Postfix overrides the fill float + lerps `_MultiplyColor` on the node's renderer. Event-driven registry (fill>0 add / →0 remove) re-pumped by a throttled driver only on dusk/dawn weight-move or live config change (no per-frame FindObjects). Verified in-game (trees + metal nodes fade; live toggle works). Diagnostics behind `Verbose` (default off).

- **CamControl → 1.1.0** (mod 79, file 7627328) — **PUSHED 2026-07-07** (`publish-all --send`: upload ✓, requirements [204] API ≥1.7.0, changelog [200], details [200]). Dev master. F7 hide-HUD now FULLY hides all UI — sweeps every enabled `Canvas` (vanilla map-layers toolbar + all mod windows e.g. Flares), disabling the component + recording exactly which it touched to restore precisely; was only driving the game's `MainUILayer` which left the toolbar/mod UI on screen. New `UI/FullHide` config (default on); off reverts to MainUILayer-only. One-shot on keypress (a window opened after hiding isn't caught — re-press to re-snapshot). Verified in-game.

- **SmartWorkerRedist → 1.2.0** (mod 37) — **PUSHED 2026-07-07** (`publish-all --send`: upload ✓, requirements [204] API, changelog [200], details [200]). Dev master 1be1322. Workers auto-haul loose ground items (issue #15). Full-replace Prefix on `PlaceableStructure.DropResources` → cancelled-build refunds spawn as worker-gatherable `Debris` (via `DebrisFactory`) instead of squad-only loose `Resource`s; no dup (no loose Resource created), native `saveToMapData` persistence, vanilla fallback if factory unresolved. Plus a base-area sweep converting EXISTING loose piles within `SweepRadius` of `HqController.MainHq` (enumerates `ResourcesController.Resources` registry — no FindObjects; spawn-then-Destroy = no dup/loss; skips squad-carried/area-work/meat+sci). Sole patcher of DropResources (ClayPitFixes patches a different PlaceableStructure method). Verified in-game.

- **PerfPack → 1.5.4** (mod 35) — **PUSHED 2026-07-07** (`publish-all --send`: upload ✓ v1.5.4, standalone/no requirements, changelog [200], details [200]). Dev master 4db7399. Rain/snow particle toggles (`[Weather]` `DisableRainParticles`/`DisableSnowParticles`, default off; issues #37/#38). `rain` is a `RainController` subclass overriding `Play`/`SetRateOverTime` → base-only patch missed it; patch both, split by type identity. Particles REMOVED via Postfix `Stop(StopEmittingAndClear)` (emission-zero-only first attempt left them frozen); `WeatherParticleDriver` clears once on flag flip for live mid-rain toggle. Sound untouched (separate `ThundersController`). Verified in-game.

- **IFZQualityOfLife → 1.6.2** (mod 33, file 7626704) — **PUSHED 2026-07-07** (`publish-all --send`: upload ✓, requirements [204] API ≥1.7.0 + Panels ≥0.1.1, changelog [200], details [200]). Dev master 4db7399. Worker large-step (issue #25): hold rebindable modifier (default Ctrl) while clicking +/- max-worker buttons to step by `WorkerLargeStep` (default 100); Shift still 10, plain 1. Full-replace Prefix on `Increase`/`DecreaseMaxWorkers`, vanilla clamp + button + priority-switcher logic reimplemented verbatim. Sole patcher of `WorkersCountChanger`. Verified in-game.

- **Elder Population (ElderPop) → 0.1.1-hotfix** (mod 81, file 7624268, existing DRAFT page) — PUSHED 2026-07-06 (`publish-all --send`: upload ✓, requirements [204] API >=1.7.0 + Panels >=0.1.1, changelog [200], details [200]). Dev repo commit bd0dafd on master. **Hotfix: fresh colonists no longer die on day one.** Citizens on a brand-new game, and later immigrants on any colony, were seeded from the full 18-78 age pyramid; an elderly-seeded pop whose rolled personal lifespan was already behind its seed age died at its first sunrise (reported: "every run, end of first day ppl die"). Fresh arrivals now seed young (18-30, below frailty age) via new `NewGameMaxDay`/`NewGameSeedMaxAge` config; the established-save adoption baseline (realistic age pyramid) is unchanged. Auto-heals across reloads, no save-format change. Still a DRAFT page -- user publishes.
- **IFZ Mod API → 1.7.0** (mod 42) — PUSHED 2026-07-06 (`publish-all --send`, upload ✓ + changelog [200] + details [200]; standalone). Adds the SicknessCoordinator **onset gate** (capability + grace/ramp): mod-driven sickness (ExtendedHealth job-illness, ElderPop frailty) stays off until the player owns a building that can treat citizens, then eases in over grace+ramp. Additive/backward-compatible — all other API consumers unaffected. Re-staged the real 1.7.0 dll into dist `plugins/000_IFZModAPI.dll` before push (dist copy was stale 1.6.2 again — same trap as last time). Followed by re-anchoring **ExtendedHealth (80) + ElderPop (81) requirements to API ≥1.7.0** via new `push-requirements.mjs --rebump` (both [204]) so players pulling the gate get the right API floor (the default idempotent path leaves a stale min).
- **IFZ Mod API → 1.6.2** (mod 42) — PUSHED 2026-07-05 (`publish-all --send`, upload+changelog+details 200; standalone). Radio vanilla-bleed fix. Gated the ElderPop/ExtendedHealth batch. NOTE: dist `plugins/000_IFZModAPI.dll` had been left stale (pre-1.6.2) by a mods.json-only bump — re-staged the real 1.6.2 dll before push (preflight caught it).
- **Buildable Bridges (BridgeProto) → 0.2.1.0** (mod 67) — PUSHED 2026-07-05 (`publish-all --send`, requirements [204] API link, changelog+details 200). Long/diagonal bridges walkable again (NavWidth decouples ribbon from deck).
- **CamControl → 1.0.0** (mod 79) — PUSHED 2026-07-05 (`publish-all --send`, requirements [204] API, changelog+details 200). Smooth eased camera (continuous zoom / slow rotation / pan) + slow-mo F8 (also slows units, via TimeChangerController) + hide-HUD F7. Defaults baked from user tuning. ✅ banner uploaded (curl-impersonate firefox147 defeats the CF JA3 gate node fetch cannot). User: set main image.
- **Elder Population (ElderPop) → 0.1.0-experimental** (mod 81, NEW page — DRAFT, user publishes) — PUSHED 2026-07-05 (`publish-all --send`, requirements [204] = API ≥1.6.2 + Panels ≥0.1.1, changelog+details 200). Population life-cycle (age→retire→frail→die). Rebuilt fresh vs API 1.6.2. ✅ banner uploaded (curl-impersonate). ⚠️ page is a DRAFT — user must publish + set main image.
- **Extended Health (ExtendedHealth) → 0.1.0-experimental** (mod 80) — PUSHED 2026-07-05, **page PUBLISHED / live 2026-07-07** (`publish-all --send`, requirements [204] API, changelog+details 200). Occupational-health sickness + 24/7 medbay/hospital w/ night-shift roster (resolved issue #2). Rebuilt fresh vs API 1.6.2. Banner uploaded (curl-impersonate).

- **IFZ Mod API → 1.6.1** (mod 42) — PUSHED 2026-07-05 (`publish-all --send`, all 4 stages 200; standalone so requirements N/A). Critical fix: TypeLoadException crash on load (Nexus user report). IFZ Mod Panels 0.1.1 + IFZ Quality of Life 1.6.1 call `IFZModAPI.Radio`, but live Nexus API was still 1.4.5 (no `Radio` type) → `TypeLoadException: Could not resolve type 'IFZModAPI.Radio'` every frame → game force-quit. Pushed to restore the game for affected users. Also bundles additive features from 1.5.0 onward (SicknessCoordinator, CombatDamageMultiplier, Radio.Announce, SaveStore) — all backward-compatible. Superseded by 1.6.2 (radio bleed fix).

- **Squad Auto Behavior (SquadAutoBehavior) → 1.1.6-beta** (mod 39, file 7621004, all 4 stages 200/204) — PUSHED 2026-07-05 (`publish-all --send`). Rearm-routing fix: `FindNearestAmmoBuilding` previously hardcoded `ResourceID.res_ammo` for building stock checks, routing mortar squads (which use `res_explosives`/`res_grenades`) to bullets-only warehouses where they couldn't rearm. Fix: gathers the squad's real ammo currencies from each member's weapon `AmmoResourceType` and accepts only buildings holding at least one of those types; mixed squads match if any needed type is present, and vanilla refills all matching types on building entry. Completes the res_ammo de-hardcoding from 1.1.5-beta. Pure routing change — no resource dupe. Requirement set [204] resolves to IFZ Mod API >= 1.6.1 (API unchanged). NOTE: in-game Wine/save-load verification still PENDING — shipped as beta for community testing.

- **Squad Auto Behavior (SquadAutoBehavior) → 1.1.5-beta** (mod 39, file 7620941, all 4 stages 200/204) — PUSHED 2026-07-05 (`publish-all --send`). Ammo-type fix: resupply now uses each weapon's actual ammunition type instead of hardcoding `ResourceID.res_ammo`, so mortars refill from their own shell stock (not bullets). Previously the system drained bullets into non-bullet weapons while refilling mortars "for free" (shell-wise) — a wrong-currency dupe. Also changed early `return` to `continue` in the resupply loop so one squad member's empty ammo type doesn't abort resupply for the rest. Requirements resolved to IFZ Mod API >= 1.6.1 (current live API). NOTE: in-game Wine/save-load verification still PENDING — shipped as beta for community testing.

- **Expanded Farming (GreenhouseGrow) → 0.1.3** (mod 72, file 7442, upload+changelog+details 200, requirements N/A) — PUSHED 2026-07-05 (`publish-all --send`). Bigger farms now turn over proportionally more: per-cycle production output and storage capacity both scale with footprint area (tier squared), so a 4x farm yields and stores ~16x a 1x (previously output was flat regardless of size). Standalone (no `requires` → requirements step N/A).

- **IFZModPanels → 0.1.1** (mod 77, file 7619132, all 4 stages 200/204) — PUSHED 2026-07-05 (`publish-all --send`). **NRE-on-close fix:** `ModWindow.Save()` ran `CaptureState()` reading a destroyed `_root` (Unity destroyed-but-non-null) → NRE + game bug-report when closing a panel after a scene reload / DestroyWindow. `Save()` now bails when `_root == null`. Fixes a bug every LIVE 0.1.0 user hit. Requires IFZModAPI.
- **IFZQualityOfLife → 1.6.1** (mod 33, file 7619135, all 4 stages 200/204, now requires IFZ Mod Panels >= 0.1.1) — PUSHED 2026-07-05 (`publish-all --send`). **Log-spam fix:** added `Plugin.Verbose` (default off) + `LogDebug`; gated `SmartWorkerRedistribution` (auto 30-GTS timer) + all `ShellLairs` operational logs that LIVE 1.6.0 spammed into `LogOutput.log`. No gameplay change from 1.6.0. Requires IFZModPanels + IFZModAPI.
- **High Ground → 1.0.3** (mod 64, file uploaded, stages 200) — PUSHED 2026-07-05 (`publish-all --send`). Log-spam hotfix: per-shot `[ElevatedFiring]` + per-refresh `[HeightAdvantage]` logs were unconditional → flooded LogOutput.log in combat; now gated behind `ElevatedFiring/Debug` (default off) via new `Plugin.LogDebug`. No gameplay change from 1.0.2.
- **High Ground → 1.0.2** (mod 64, superseded by 1.0.3) — PUSHED 2026-07-05 (`publish-all --send`). Range-boost fix: height advantage now applies to actual firing range (garrisoned units shoot at the boosted range, not just see it) — root cause was boosting only `Group.FightRange` (cone + detection) while the real fire gate `CharacterFightHandler.IsInAttackRange` compares per-weapon reach; new sole Postfix on `CharacterFightHandler.GetWeaponAttackReach`. Visual: green cone = base range, orange ring = height-bonus extension (was a single recolored cone). Thanks weeman12321 for reporting. Standalone.

- **GreenhouseGrow → 0.1.2** (mod 72) — PUSHED 2026-07-05 (`publish-all --send`). Build/deconstruct crews now scale with farm footprint area (tier squared), matching cost/staff/yield — bigger farms get proportionally more builders and demolition crew; build time stays about the same. Fixed: farm sizes were lost on reload when the save was reloaded on a later in-game day than the farms were placed (sidecar key was keyed on the save name, which embeds the in-game clock, and drifted) — now keyed on map coordinates (clock-independent), with a one-time auto-migration recovering existing saves. No longer requires IFZ Mod API — standalone now (no `requires` → requirements step N/A).

- **High Ground → 1.0.1** (mod 64, file uploaded, all 4 stages 200) — PUSHED 2026-07-04 (`publish-all --send`). ElevatedFiring fix: the lifted shot origin was blocked by the unit's OWN building shell — native `SetEnterable` exempts the own building only on the RETURN ray, not the forward origin→target ray, so `CanShot` returned false and elevated units couldn't shoot over anything. New `IgnoreOwnBuilding` (default on) adds the own `ShotBlockingCollider` to `IgnoredColliders` on enter (exempts both rays), removes it on exit. `ElevateFovCone` lifts the on-screen FoV/range cone to the firing floor so it clears shorter buildings; `Debug` toggle added. Standalone (no `requires` → requirements step N/A).

- **GreenhouseGrow → 0.1.0** (mod 72, file 7616210, NEW PAGE — published) — PUSHED 2026-07-04 (`publish-all --send`, requirement + changelog + tags applied). New mod: build farms and greenhouses at a chosen size with `,`/`.` keys to cycle x1/x2/x3/x4 size tiers (live preview). Cost, staff capacity, and daily production scale with footprint area (tier squared), so a 2x farm costs and yields roughly 4x. Works on open-field farms and greenhouses; size persists per save. Verified live via Nexus API (status: published, v0.1.0, file size matches). Requires IFZModAPI 1.4.5+.

- **IFZ Mod API → 1.4.5** (mod 42) — PUSHED 2026-07-02 (`publish-all --send`, all stages 200/204). New additive event `Cache.CharacterDied`, fired from sole Prefix on `Character.OnCharacterDeath`. Backward-compat audited: public-surface additions only, zero breaking changes; older mods unaffected. Hard dependency for BlitzHund 0.1.0.

- **BlitzHund → 0.1.0** (mod 71, file 7613702, NEW PAGE — DRAFT, user publishes) — PUSHED 2026-07-02. Suicide-bomber infected-dog variant: configurable fraction (default 15%, `BlitzChance`) of killed dogs detonate using native explosion, dealing area damage to player squads and structures. Config: `Enabled`, `BlitzChance`, `BlitzDamage` (0=clone native), `BlitzRadius` (0=clone native), `DogDraftName`, `Cascade` (chain-detonate nearby bombers; default on), `DebugLog`. Sole-patcher of `Character.CustomFactory.Create`; IFZModAPI sole-patches `Character.OnCharacterDeath`. No collisions. Requires IFZModAPI 1.4.5+. Requirement [204], changelog + details [200], banner + 2 gallery images uploaded. User sets main image + publishes draft.

### 2026-06-30 (cycle 10)
- **Squad Auto Behavior (SquadAutoBehavior) -> 1.1.4-beta** (mod 39, file 7598132, API req set [204]) -- PUSHED. Idle-gate fix: auto-behavior tick skips squads with an active player order (CurrentOrder != null); prevents 8s-tick from inserting rearm/hospital order at front of queue mid-player-drive, eliminating stutter during mass-deconstruct. NOTE: in-game Wine/save-load verification still PENDING -- shipped as beta for community testing. Requires IFZModAPI >=1.4.4.

- **Gunfire Lights → 1.4.10** (mod 30, file 7593788) — PUSHED 2026-06-28. Cinematic defaults baked in as shipped: muzzle intensity 5.5 → 115.5, MuzzleSize 0.8 → 1.1, ExplosionSize 3.5 → 4.5, OneLightPerTower false (per-barrel searchlights). No code change. Requires API ≥1.4.4.

### 2026-06-29 (cycle 9)
- **Mass Deconstruct (MassDeconstruct) -> 1.4.0-beta** (mod 58, file 7597706, API req set) — PUSHED. Live footprint highlight: bold red fill over each building in the drag box (previously selection was invisible). Drag now uses native game-camera world-point — fixes offset on elevated/real-world-tile maps. Also includes 1.3.0 pacing (<=50 concurrent deconstructs, prevents navmesh crash on mass-clear). NOTE: changelog + page-details steps hit Cloudflare 403 — retry push-changelog.mjs + push-details-api.mjs when cookie refreshed. Requires API 1.2.1+.

### 2026-06-28 (cycle 8)
- **IFZ Quality of Life Bundle → 1.5.2** (mod 33) — PUSHED (beta). Mortar squads and bunkers shell infected lairs and buildings with infected inside via right-click (native attack-ground: real ammo, low-ammo warning, retreat, auto-route); auto-exit vehicle or building first; right-click elsewhere cancels. HMG vehicle ammo-drain fixed (trunk refill rate-matches vanilla). Worker-update reflection cached (removes per-frame cost spike). Caveats: may fire a few rounds post-clear; premature vehicle exit; watch low-ammo squads. Requires IFZModAPI.
- **Performance Pack → 1.5.2** (mod 35) — PUSHED. HUD adds active lair count and, on low FPS, a sticky "why" cause line (`[category]:[reason]`). Reads existing engine counters only; no cost when hidden.
- **Buildable Bridges (BridgeProto) → 0.2.0.1** (mod 67) — PUSHED (beta). Native bridge builder: real road/deck/railing textures and per-span deformation. Requires IFZModAPI.
- **Clay Pit Fixes → 1.1.0.0** (mod 65) — PUSHED. Demolished clay-pit area regenerates production over time and persists across save/reload.
- **NoPath - YesPath → 0.3.0** (mod 68) — PUSHED. Denser demolish footprint sampling and reachability checks so workers are not assigned unreachable spots; new config (max work positions, sample spacing, inside-only). Requires IFZModAPI.
- **Squad Auto Behavior → 1.1.3** (mod 39) — PUSHED (file 7591004, API requirement set [204], details [200], tags=2). Dry/wounded squads under fire were pinned in place: `IsTargeted()` read as a combat order, blocking auto-return to rearm/hospital. Fix: while targeted, falls back only if the nearest rearm/hospital building is within new `OverrideNearCoverMeters` (default 80 m; 0=never). Far-from-cover squads hold as before. Applies to rearm and hospital paths. Requires IFZModAPI.

### 2026-06-26 (cycle 7)
- **IFZ Mod API → 1.4.4** (mod 42) — PUSHED (file 7586032, details [200]). Harmony __args compatibility fix: removes dependency on newer HarmonyX `__args` injection, so patches apply cleanly on old BepInEx (5.4.5.0). Fixes GiveOrder patch failure on Chinese-repo old versions (SquadMerge merge hook never ran). Public-surface unchanged; backward-compat verified.
- **Buildable Bridges (BridgeProto) → 0.1.1** (mod 67) — PUSHED (file 7585955, API ≥1.4.3 requirement set, details [200]). Bugfix: placed bridges were indestructible — the deck-finish step disabled the wall's BoxCollider so the van could cross, but a disabled collider is invisible to ALL physics queries, so shelling/explosions (`AreaDamageUtilities.ApplyAreaDamage` → `OverlapSphere` on "Wall") and the deconstruct tools (vanilla + MassDeconstruct → `OverlapBox` on "Wall") both skipped it. Now `bc.isTrigger = true` instead — overlap queries still return triggers (`queriesHitTriggers` on) but they generate no collision response, so traffic still crosses; `VehicleCollisionHandler` only raycasts the "Building" layer so it never reads the bridge as an obstacle. Old saves self-heal (load re-runs the finish step). Requires API ≥1.4.3.
- **House Rebalance → 1.2.0** (mod 32) — PUSHED (file v1.2.0 created + details [200], tags=2). Standalone mod (no API requirement → requirements step N/A). Adds `[Commute] PreferShortCommute` (default OFF — classic behaviour unchanged): among houses of a worker's best-attainable tier, prefer the one nearest their workplace. Housing tier stays the hard rule (never moved to a worse house → no 'want better housing' mood penalty). `[Commute] ImprovementMeters` hysteresis (def 15m) stops tick-to-tick bouncing; tier upgrades ignore it and always move. Note: `publish-all` aborted at the requirements step (standalone mod has no `requires` array — fatal-on-empty); finished via `push-details-api.mjs --send` since there were no requirements to set.
- **Unlocked Buildings (was "Split Unlock") → 1.2.0** (mod 38) — PUSHED (details only, [200], tags=2). Rename + metadata refresh, same version (1.2.0 DLL already live → no file re-upload). Page title `Split Unlock - castles and cathedrals` → **Unlocked Buildings** (key/dll/modId unchanged). Description/summary/mainFeatures/fileNotes rewritten for the full feature set: split bypass (`AllowWrongDimensions` / `AllowSmallSurface`) **+** custom-building restrictions lifted (`[BuildingSize]` size override **and** `AllowShortWalls` min-wall-length bypass, which the old page never documented). README table label updated. No API requirement change.
- **Squad Grenades → 1.0.0** (mod 69, NEW PAGE) — PUSHED (file 7582112, API ≥1.4.3 requirement set, details [200]). New mod: idle player squads auto-lob grenades + auto-fire mortars at hostile clusters ALREADY in range (never advancing), incl. from inside buildings AND moving vehicles (vehicle squads fire the accessory directly so they keep driving). Friendly-fire safe — cancels if the blast would catch your squads or any non-hostile group (survivors/civilians/allied army). Vanilla never auto-throws grenades. Sole patcher of `GroupsController.CustomUpdate`. Banner uploaded. **Page is a DRAFT — user publishes it.** Requires API ≥1.4.2.
- **Surrounded → 0.4.5** (mod 55) — PUSHED (file 7582040, API ≥1.4.3 requirement set, details [200]). Early-game easing for lair waves (the 0.4.4 release pump fired on day 1 of a fresh base / new expedition from pre-existing/seeded lairs). Adds `LairReleaseGraceDays` (def 5, no waves before that RUN-day — mirrors RaiderEscalation grace), `LairWaveRampDays` (def 20, wave budget ramps 0→100% over grace→grace+N via `rampedCeiling`), `LairWaveCooldownHours` (def 8, spaced waves not a drip). All RunDay-based (resets per expedition), self-healing (recomputed each pass, no latch), fail-open if RunDay throws. Requires API ≥1.4.2.

### 2026-06-25 (cycle 6)
- **RaiderEscalation → 1.2.6** (mod 57) — PUSHED. Early-game grace period: new `[Raids] GraceDays` (default 7) gates the raid check on `day >= GraceDays`, so a strong pre-existing camp that seeds straight to raid tier can no longer wipe a fresh base on day 2-3 (reported by HowNowYellowCow). `GraceDays=0` restores old behaviour. Also declared the long-standing 000_IFZModAPI hard dependency in mods.json + README. Requires API ≥1.2.1.
- **Surrounded → 0.4.4** (mod 55) — PUSHED. Lair-release fix: `SiegeRelief.LairReleasePass` used `fireAt = Max(LairReleaseThreshold, cap*atFrac)`, so a threshold ≥ lair capacity (user's 150 vs cap 149) made the trigger unreachable — pump never fired, lairs sat full, infected stood idle instead of assaulting the HQ (`TotalLairReleased=0`). Now the floor only SKIPS small lairs (`if (cap < floor) continue`), `fireAt = ceil(cap*atFrac)`. Verified in-game (PerfPackDev F8 census): `TotalLairReleased` 0→873, attacking-HQ 180→810 moving. Requires API ≥1.4.2.

### 2026-06-24 (cycle 4)
- **IFZ Mod API → 1.4.3** (mod 42) — PUSHED. Fixes the base-game "0 <month>" clock flash on load (the restore patch fired `SetDaysFromPreviousGames` unconditionally → vanilla `DaySetter` painted the event arg as the day; now skips the no-op call). Internal-only change — **backward-compat audited: zero public-surface change vs 1.4.2, every API member used across the pack still resolves → older mods unaffected.**
- **Surrounded → 0.4.3** (mod 55) — PUSHED. Swarmpile FPS fix: `SiegeRelief` de-virt throttle on `Swarm.ConvertGroup` (`MaxLiveInfected`, surplus stays virtual) + roaming cull + concurrent-swarm cap (30) + radio throttle; HUD shows real live count. FPS 1→25-40 on the day-253 save. Requires API ≥1.4.3.
- **CinematicFX → 1.2.0** (mod 25) — PUSHED. Collapsed from 4-part dev (1.1.5.03). Tracers reworked onto a shared additive material so they glow under the night Bloom; warm tint, tail-fade, `PauseFreeze` for screenshots. Requires API ≥1.4.3.
- **BridgeProto → 0.1.0** (mod 67, NEW PAGE) — PUSHED. New experimental mod "Buildable Bridges (Experimental)": drag-build a walkable bridge over water with the wall tool (B). Banner uploaded. Page is a DRAFT — **user publishes it**. Wine-safe (one drag-only patch, lifecycle via events). Requires API ≥1.4.3.

### 2026-06-23 (cycle 3)
- **Surrounded → 0.4.2** (mod 55) — PUSHED (hotfix). `MaxHordeMultiplier` (default 3, 0=off) clamps the vanilla `HordeSizeMultiplier` getter, which corrupts/inflates on long saves (~2.9e8 @ day ~250) and pins every swarm to max — the megablob. Clamp runs even with the siege boost off. Requires API ≥1.4.2.
- **CinematicFX → 1.1.4** (mod 25) — PUSHED. Tracer buff was a no-op (targeted `LineRenderer`; bullet tracers use a `TrailRenderer`); now widens the trail renderers so tracers glow again via the night Bloom. Requires API ≥1.4.2.

### 2026-06-23
- **WindowGlow (Night Lights) → 1.0.0** (mod 66) — PUSHED. New mod: warm amber window lights at night via an embedded Built-in-RP shader bundle (single self-contained DLL); standalone. Banner uploaded. Page is a DRAFT — **user publishes it on Nexus**.
- **GunfireLights → 1.4.9** (mod 30) — PUSHED. Searchlights skip weaponless structures (masts/statues); cinematic defaults baked in. Requires API ≥1.4.2.
- **HousePower → 1.1.0** (mod 31) — PUSHED. Mood bonus reworked into a real visible +X% row, idempotent (no compounding), per-resident wood. Requires API ≥1.4.2.
